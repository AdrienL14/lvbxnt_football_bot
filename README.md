# Lvbxnt Football Bot — V4.2 ELITE FINALE

Use `.env.example` as `.env`, then:

```powershell
python -m venv .venv
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
python app.py
```
